<metal:page use-macro="context/@@getpaid-content-template/macros/content_space"
            i18n:domain="getpaid.nmi">
  <metal:block fill-slot="getpaid_content">
      <h1 class="documentFirstHeading" tal:content="view/label">Title</h1>
	  <dl class="portalMessage error" style="display: none">
            <dt>Error</dt>
            <dd>Please correct the indicated errors.</dd>
      </dl>
      <div>
      	<script>
      		<!--
      		jq(document).ready(function(){
	      		jq('#skel-contents form').submit(function(){
					cont = true;
					if (jq('#ccnumber').val() == '') {
						jq('#ccnumber').parent().parent().addClass('error');
                        jq('.submitting').removeClass('submitting');
						cont = false;
                        jq('.portalMessage').show()
				    } else {
						jq('#ccnumber').parent().parent().removeClass('error');
					};
                    if (jq('#ccexp').val() == '') {
                        jq('#ccexp').parent().parent().addClass('error');
                        jq('.submitting').removeClass('submitting');
                        cont = false;
                        jq('.portalMessage').show()
                    } else {
                        jq('#ccexp').parent().parent().removeClass('error');
                    };
					return cont;
				});
			});
			-->
      	</script>
	      <div id="skel-contents">
	        <span tal:replace="structure view/contents" />
	      </div>
      </div>
  </metal:block>
</metal:page>